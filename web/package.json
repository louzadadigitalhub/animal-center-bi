{
  "name": "animal-center-bi",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host --port 5173",
    "build": "vite build",
    "preview": "vite preview --host --port 5173",
    "copy:maplibre": "node scripts/copy-maplibre-worker.mjs",
    "predev": "npm run copy:maplibre",
    "prebuild": "npm run copy:maplibre && npm run check:tokens && npm run check:classes",
    "check:tokens": "node scripts/check-tokens.mjs",
    "check:classes": "node scripts/check-classes.mjs"
  },
  "dependencies": {
    "@phosphor-icons/react": "^2.1.10",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "maplibre-gl": "^6.9.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-map-gl": "^8.1.3",
    "sileo": "^0.1.5",
    "tailwind-merge": "^3.6.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "@vitejs/plugin-react": "^4.3.4",
    "tailwindcss": "^4.3.3",
    "vite": "^6.0.0"
  }
}
